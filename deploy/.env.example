# 复制为 .env 后按需修改： cp .env.example .env

# 管理端 Bearer 令牌（登录 Web 控制台用）。留空则首启自动生成并打印到容器日志。
CFST_API_TOKEN=

# 允许跨域来源，逗号分隔或 *
CFST_CORS_ORIGINS=*

# 日志级别 trace|debug|info|warn|error
CFST_LOG_LEVEL=info

# 公开模式：对外优选 IP API 接受任意 key（无缝迁移旧 wetest/hostmonit 客户端时设为 true）
CFST_PUBLIC_OPEN=false
